# Frontend configuration for the Flutter app.
# This is the backend API base URL that the app will call.
# Do not put database passwords, JWT secrets, or private backend credentials here.
API_BASE_URL=https://qa-api.orderx.online
